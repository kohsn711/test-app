# 08 記録詳細

## 概要
学生が過去の記録を閲覧する画面。監督・保護者も同じ詳細ページを閲覧。

## ToDo

### 学生用詳細（`app/(student)/records/[date]/detail/page.tsx`）
- [x] 日付ヘッダー表示
- [x] 各カテゴリの記録をセクション別に表示
- [x] 入力済みカテゴリのみ表示（未入力は非表示）
- [x] 「編集」ボタン（本人のみ）
- [x] リアクション・コメントの表示

### データフェッチ
- [x] `daily_records` とすべての子テーブルを一括取得
- [x] リアクション・コメントを取得（`lib/daily-record.ts` の `fetchRecordSocial`）
- [x] `Promise.all` で並列フェッチ
- [x] `params` は `await params` で取得

### アクセス制御
- [x] 本人のみ閲覧可（それ以外は `notFound()`）
- [ ] コーチ用・保護者用の詳細ページは別ルートで実装（09・12参照）

### UI
- [x] スマホファーストのレイアウト
- [x] `loading.tsx` でスケルトン表示
- [x] `error.tsx` でエラー表示

## 備考
- ケガ・健康情報は医療判断として扱わない旨の注意書きをケガセクションに表示済み
