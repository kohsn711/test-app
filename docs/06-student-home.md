# 06 学生ホーム

## 概要
学生のメイン画面。記録継続を促すダッシュボード。BottomNavの「記録」タブに対応。

## ToDo

### 画面（`app/(student)/home/page.tsx`）
- [x] 今日の記録ボタン（当日記録済みかどうかで表示を切り替え）
- [x] 今日のひとこと（固定メッセージまたはランダム励ましメッセージ）
- [x] カレンダー（記録済み日にマーク表示）
- [x] 連続記録日数表示
- [x] 最近のリアクション/コメント一覧（最新3〜5件）

### データフェッチ
- [x] 当月の記録済み日付一覧を取得
- [x] 連続記録日数を計算
- [x] 最新のリアクション・コメントを取得
- [x] 未読通知件数を取得（PageHeader ベル表示用）
- [x] 独立したフェッチは `Promise.all` で並列実行

### UI
- [x] スマホファーストのレイアウト
- [x] `loading.tsx` でスケルトンUIを表示
- [x] カレンダーコンポーネント（Client Component）
- [x] `PageHeader`（`sticky top-0`）にグリーティング・チーム名・通知ベルを配置

## 優先表示順（現行）
1. 今日の記録ボタン
2. 今日のひとこと
3. カレンダー
4. 連続記録日数
5. 最近のリアクション/コメント

## 備考
- 目標進捗セクションは BottomNav「目標」タブで代替のため削除済み
- コンテンツ導線・保護者登録セクション・旧通知ボタンは重複のため削除済み
- チーム未参加時のみ `/team/join` への案内テキストをヘッダー下に表示
- 通知ベルは `PageHeader` 右端にインライン配置。`countUnreadNotifications` で未読件数取得
